# Track large dataset files with Git LFS
data/apps/**.zip filter=lfs diff=lfs merge=lfs -text
# Optionally track CSVs too (uncomment if your CSVs are large)
# data/apps/**.csv filter=lfs diff=lfs merge=lfs -text
